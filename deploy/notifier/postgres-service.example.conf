# Safe syntax example only. Copy outside the repository, replace every
# CHANGE_ME value through the approved controlled channel, then provision it
# for container UID/GID 65532:65532 at mode 0400 as described in README.md.
# The password belongs in the separate libpq passfile, never in this file.
[brerc-notifier]
host=CHANGE_ME_PRIVATE_DATABASE_DNS
port=5432
dbname=CHANGE_ME_DATABASE
user=CHANGE_ME_NOTIFIER_LOGIN
sslmode=verify-full
