# Deployment

> **Skeleton — fill this in as you build (see `BUILD_PLAN.md`).**
> Running locally (docker-compose) and in production; same-origin embedding + CSP frame-ancestors; TLS; the basemap; and how dev→prod is a credentials-only change (PUBLIC_DATABASE_URL).
> Reference: Learning Guide Module 8 · Answer Key: `brerc-public-dashboard/docs/DEPLOYMENT.md`

## TODO
- [ ] Write this document as the corresponding code settles.
- [ ] Keep terminology consistent with `docs/DATA_GOVERNANCE.md` and `CLAUDE.md`.
