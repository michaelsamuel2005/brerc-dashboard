"""Pydantic response models — the API's data contracts.  TODO (build this).

Define the shapes the endpoints return (keep them in step with web/src/types.ts):
  SpeciesMatch, FilterOptions, SummaryStats, OccurrenceCell, OccurrenceCells,
  SpeciesInfo.

Learning Guide -> 04-the-api.md.
Answer Key: brerc-public-dashboard/api/app/models.py
"""
# TODO: define the models here as you build the endpoints (Module 4).
