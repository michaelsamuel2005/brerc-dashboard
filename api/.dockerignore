# Allowlist the complete Docker build context. Only dependency metadata and
# Python files belonging to the read-only app may reach the daemon.
*
!pyproject.toml
!app/
!app/**/
!app/*.py
!app/**/*.py
