export function MapInstructions() {
  return (
    <p className="map-instructions">
      <strong>Using the map.</strong> Tap or click a shaded square for details. Use +/− to zoom and the arrow buttons
      to move the map. On a touchscreen, use two fingers to move it. You can also choose a square in the table below.
    </p>
  );
}
