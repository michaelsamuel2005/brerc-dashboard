// The API / UI data contracts.  TODO (build this) — Learning Guide Module 4/5.
//
// Define and export: SpeciesMatch, FilterOptions, SummaryStats, OccurrenceCell,
// OccurrenceCells, SpeciesInfo, Filters, and EMPTY_FILTERS. Keep these in step
// with api/app/models.py (a change to one changes the other in the same PR).
// Answer Key: brerc-public-dashboard/web/src/types.ts
export {};
