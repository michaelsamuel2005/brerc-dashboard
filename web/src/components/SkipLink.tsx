/** A skip link so keyboard users can bypass the header and filters (WCAG 2.4.1). */
export function SkipLink() {
  return (
    <a className="skip-link" href="#main-content">
      Skip to map and records
    </a>
  );
}
