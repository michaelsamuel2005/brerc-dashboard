// Typed client for the read-only API + the Martin tile-URL builder.  TODO.
//
// Build: a small fetch wrapper, an `api` object (summary, filters, searchSpecies,
// cells, speciesInfo), `filtersToParams`, and `tileUrlTemplate(filters)`.
// Learning Guide Module 4/5.
// Answer Key: brerc-public-dashboard/web/src/api/client.ts
export {};
